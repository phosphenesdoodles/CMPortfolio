// Function to toggle the visibility of the text box when the cover is clicked
function toggleInfo(coverElement) {
  // Toggle the "active" class on the clicked cover section
  coverElement.classList.toggle('active');
}

